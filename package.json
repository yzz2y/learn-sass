{
  "scripts": {
    "serve": "live-server --port=8090 --host=localhost",
    "sass": "sass src/syntax/input.scss src/syntax/output.css",
    "watch": "npm run sass -- --watch",
    "update": "npm run watch -- --update"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.80.3"
  }
}
